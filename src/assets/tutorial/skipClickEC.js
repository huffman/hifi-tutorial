(function() {
    var utils = Script.require('./utils.js');

    this.startNearTrigger = utils.sendSkipTutorialMessage;
    this.startFarTrigger = utils.sendSkipTutorialMessage;
    this.clickDownOnEntity = utils.sendSkipTutorialMessage;
});
